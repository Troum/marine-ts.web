import type { ThemeTitleFontWeight } from '~/types'

/** Допустимые numeric `font-weight` в rich-text и TFT-редакторах админки. */
export const TIPTAP_FONT_WEIGHT_VALUES: ThemeTitleFontWeight[] = [400, 500, 600, 700, 800]
